# 新系统实际Demo深测（2026-09-15）

环境：`/root/auto-trader-longtime-02`；固定Bybit Mainnet Demo，未切换资金实盘。

## 已实际验证

- 新账户认证、全仓状态、余额读取、空仓与无在途订单；BTC/ETH双向持仓模式。
- 独立Bot身份、无webhook冲突；4条消息投递SENT，包括模型额度告警及用户命令回复。
- 真实公开行情、六币筛选、方向模型、止盈可达性检查。
- 第一轮筛选引用跨币证据，被严格校验拒绝，无下单；保持原校验不放宽。
- 第二轮POWER无方向、AKE止盈不可达，均正常跳过。
- 部署周期1491223：LSK通过策略并提交开空77.8枚，交易所返回Cancelled，cumExecQty=0，rejectReason=EC_NoImmediateQtyToFill。账本UNFILLED，无重复提交。
- 后续AKE模型额度不可用，本轮停止后续分析并告警。
- 独立systemd服务已active、enabled，NRestarts=0；交易启用，5秒监控正常。
- 进程锁阻止第二个交易进程；正常重启后周期、订单ID、尝试次数保持一致，无重复分析或下单。
- 最后核对无仓位、无挂单。服务继续运行；模型额度恢复后由正常周期重新分析。

## 深测发现并修复

1. 全部对冲成交后的撤TP不能被逐笔历史接口故障阻塞。已把通知证据获取与交易状态推进分开。
2. 跨过30秒窗口后才观察到全成时，按逐笔时间恢复部分/全成通知；不会把较慢全成误记为30秒内全成。
3. 通知待补证据独立保留原对冲轮次，重新判向后不会错归到下一轮。
4. 验收脚本改为核对交易所实际委托及整个对冲组结束，不将主仓结束误当作全流程结束。

## 证据

- `runtime/trader.db`：真实行情、模型输入输出、订单回执、事件与通知账本。
- `runtime/live-depth-check.json`：最新只读交易所核对。
- `runtime/restart-verification.json`：重启前后周期与订单对比。
- `runtime/demo-acceptance.json`：第二轮无合格开仓结果。

## 尚未实际验证

开仓实际成交、TP与对冲同时存在、部分/全部对冲成交、30秒通知时序、全成撤TP、双仓重新判向后的平逆向仓/重挂、顺向先止盈后平反向余仓及真实盈亏结算。
这些分支有离线测试覆盖，但目前没有自然成交证据，不能声明全都符合预期。
当前外部阻碍：模型额度不可用，以及已提交Demo市价单未获得流动性成交。未改策略阈值或强制方向以制造验收结果。

## 最终离线回归

302项测试通过（29.19秒）；Ruff通过；mypy检查42个源文件通过。

## 额度恢复后完整周期复验

周期 `verification:6eaff68405a9ff43d1cfff4b` 实际运行成功，真实筛选、方向分析和止盈可达性检查完成。LSKUSDT、AKEUSDT均因止盈不可达跳过，无新增订单或仓位。模型额度告警自动解除，最新未解决异常为0。常驻服务恢复active，保留用户原先暂停开仓设置。证据：`runtime/recovered-cycle-check.json` 与 `runtime/live-depth-check.json`。

本轮未出现合格交易，因此对冲实际成交、双仓判向和平仓分支仍待自然交易验证；不能据此声明全系统真实成交链路全部通过。
